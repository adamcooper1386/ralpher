version: 1
binary: ../target/release/ralpher

sandbox:
  workdir: temp
  inherit_env: true

tests:
  - name: "validate with config shows not implemented"
    setup:
      - write_file:
          path: "ralpher.toml"
          contents: |
            # minimal
    steps:
      - name: run
        run:
          cmd: "sh"
          args: ["-c", "RUST_LOG=info ${BINARY} validate 2>&1"]
        expect:
          exit: 0
          stdout:
            contains: "validators not yet implemented"
