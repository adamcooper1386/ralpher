version: 1
binary: ../target/release/ralpher

sandbox:
  workdir: temp
  inherit_env: true

tests:
  - name: "iteration without task file fails"
    setup:
      - write_file:
          path: "ralpher.toml"
          contents: |
            git_mode = "branch"
    steps:
      - name: run
        run:
          cmd: "${BINARY}"
          args: ["--iteration"]
        expect:
          exit: 1
          stderr:
            contains: "No ralpher.prd.json found"
