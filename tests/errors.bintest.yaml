version: 1
binary: ../target/release/ralpher

sandbox:
  workdir: temp
  inherit_env: true

tests:
  - name: "unknown flag fails"
    steps:
      - name: run
        run:
          cmd: "${BINARY}"
          args: ["--unknown-flag"]
        expect:
          exit: 2
          stderr:
            contains: "unexpected argument"
