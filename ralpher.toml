# ralpher configuration for self-development
# This allows ralpher to drive its own development via Claude Code

git_mode = "branch"

[agent]
type = "command"
# Base command - ralpher will add -p with composed prompt including task context
# --dangerously-skip-permissions allows Claude to write files without prompting
cmd = ["claude", "--dangerously-skip-permissions"]
